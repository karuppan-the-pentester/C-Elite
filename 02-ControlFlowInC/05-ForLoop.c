// Importing the header files
#include <stdio.h>

// Creating the main function
int main(){

    // Looping using forloop
    for (int i = 0; i < 5; ++i) {
        printf("%d \n",i);
    }

    return 0;
}